import type { LLMMessage } from './types';

/**
 * Системный промпт Pulse. Упрощённый — рассчитан на маленькие локальные модели
 * (Gemma 2 2B и т.п.), которые плохо следуют длинным инструкциям. Краткий,
 * конкретный, в одно предложение по сути.
 *
 * Блок про web_search — frontend автоматически подставит <search_results>
 * в user-сообщение, если вопрос похож на «актуальная инфу». Модель должна
 * использовать заголовки и url'ы из этого блока в ответе, а не выдумывать.
 */
export const PULSE_SYSTEM_PROMPT: LLMMessage = {
  role: 'system',
  content: `Ты Pulse, дружелюбный AI-ассистент. Отвечай кратко, 2-4 предложения. На русском если юзер на русском. С эмодзи но умеренно.

Если в сообщении юзера есть блок <search_results ...>...</search_results> — это реальные результаты web-поиска. Используй заголовки и url'ы из него, не выдумывай факты. В конце ответа добавь блок "🔍 Источники:" со списком (макс. 3) в формате:
🔍 Источники:
- [точная фраза title](url)
- [точная фраза title](url)
Title бери из результатов, не перефразируй. Если результатов нет (offline) — не добавляй блок источников.`,
};

/**
 * Pulse v5 — agentic AI system prompt. Сейчас идентичен PULSE_SYSTEM_PROMPT
 * (т.к. agent v5 ещё не дописан в TS). Когда появится tool registry —
 * добавим сюда список доступных tools и формат ответа.
 */
export const PULSE_SYSTEM_PROMPT_AGENT: LLMMessage = PULSE_SYSTEM_PROMPT;
